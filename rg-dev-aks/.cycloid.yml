version: "2"
name: rg-dev-aks
canonical: rg-dev-aks
author: rboatwright
status: private
description: rg-dev-aks
image: ""
keywords:
- rg-dev-aks
- provider:azure
technologies: []
dependencies: []
config:
  default:
    name: default
    pipeline:
      pipeline:
        path: pipeline/default/terraform-pipeline.yaml
      variables:
        destination: ($ project $)/pipeline/variables-($ environment $).yml
        path: pipeline/default/variables.sample.yaml
